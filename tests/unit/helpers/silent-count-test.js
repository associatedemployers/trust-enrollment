import {
  silentCount
} from 'trust-enrollment/helpers/silent-count';

module('SilentCountHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = silentCount(42);
  ok(result);
});
