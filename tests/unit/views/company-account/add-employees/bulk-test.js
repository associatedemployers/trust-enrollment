import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:company-account/add-employees/bulk');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
