import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:employee-login/verify-id', 'EmployeeLoginVerifyIdView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
